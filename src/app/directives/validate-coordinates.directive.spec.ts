import { ValidateCoordinatesDirective } from './validate-coordinates.directive';

describe('ValidateCoordinatesDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateCoordinatesDirective();
    expect(directive).toBeTruthy();
  });
});
